req_url = "https://web.snapchat.com/#/search?result=a&form=add"

def __addbot__:
  while True:
    req_url.replace("result=a" [-] "result=alfa+1")
    if replace = z:
      print('work done')
        print('in %time.date.now-1hrs%')
        
 addbot = addbot() 
